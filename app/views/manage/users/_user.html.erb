<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, [:manage, user] %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", [:manage, user], method: :delete, data: {confirm: "You sure?"} %>
  <% end %>
</li>